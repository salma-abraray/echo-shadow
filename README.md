# wanderlost (echo-shadow)

This repo contains an Expo React Native project. To run locally:

```bash
npm install
npm run web
```

To publish as a static website, build the web bundle and deploy to Vercel/Netlify/GitHub Pages.
